# Reserved Topic — Just-in-Time Rust

This is not a generated lesson. Rust will be taught inside approved architecture lessons only when its syntax is encountered. This marker is retained to avoid silently renumbering the original scaffold.
